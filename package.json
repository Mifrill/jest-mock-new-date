{
  "name": "jest-mock-new-date",
  "version": "1.0.3",
  "description": "Just add 'mockDate(mockedDate)' as hook for jest tests scope (describe function) and use 'new Date(mockedDate)' in tests.",
  "main": "./index.js",
  "scripts": {
    "build": "rm -rf dist/ && tsc && cp ./package.json dist && cp ./README.md dist",
    "test": "jest --runInBand"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Mifrill/jest-mock-new-date.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Mifrill/jest-mock-new-date/issues"
  },
  "homepage": "https://github.com/Mifrill/jest-mock-new-date#readme",
  "devDependencies": {
    "@types/jest": "*",
    "jest": "*",
    "ts-jest": "*",
    "ts-node": "*",
    "typescript": "*"
  }
}
